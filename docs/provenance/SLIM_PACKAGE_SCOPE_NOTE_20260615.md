# SLIM_PACKAGE_SCOPE_NOTE

Created UTC: 2026-06-15T12:07:08.087855Z

This ZIP is a slim upload candidate derived from the IJRS clean package boundary. It does not alter scientific CSV/JSON result values. It removes only:

1. The raw nested VHR source archive `source_package/source_archives/advisor_verified_review_DaiWenzhe_20260611.zip`, which is not referenced by the manuscript package paths and should be kept separately if a repository/raw-evidence handoff is required.
2. Exact duplicate figure copies under `revised_artifacts/figures/`; the identical `submission_manuscript/figures/` PNG/PDF copies are retained and are the ones referenced by the manuscript and current figure audit.

Removed files are listed in `SLIM_PACKAGE_REMOVED_FILES_20260615.csv`. Current package records are regenerated from the files actually included in this slim ZIP.
